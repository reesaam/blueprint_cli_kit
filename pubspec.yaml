name: blueprint_cli_kit
description: BluePrint CLI for Flutter and Dart Projects
version: 0.0.1
homepage: https://resam.site
repository: https://github.com/reesaam/blueprint_cli_kit.git
funding:
  - https://resam.site

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.5.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
